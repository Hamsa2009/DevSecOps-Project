apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  labels:
    app: netflix-app
spec:
  type: NodePort
  ports:
  - port: 85
    targetPort: 85
    nodePort: 30008
  selector:
    app: netflix-app
